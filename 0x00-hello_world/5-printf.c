#include <stdio.h>
/**
 * main - Print a C programm with the printf function
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
